"use client";
import { TopbarProps } from "../props/topbarProps";

const Topbar = ({ children, className, style }: TopbarProps) => {
	// const [isMenuOpen, setIsMenuOpen] = useState(false);
	return (
		<div className={className} style={style}>
			{children}
		</div>
	);
};

export default Topbar;

{
	/* <div className="bg-purple-900 px-4 flex justify-between items-center  */
}
// w-screen h-16">
{
	/* <div className="flex items-center gap-5"> */
}
{
	/* {/* <Button onClick={() => setIsMenuOpen(!isMenuOpen)}> */
}
// {!isMenuOpen ? <AiOutlineMenu size={24} className="text-white" /
// > : <AiOutlineClose size={24} className="text-white" />}</
// Button> */}
{
	/* <h1 className="text-white text-3xl">Topbar</h1> */
}
{
	/* </div> */
}
{
	/* </div> */
}
